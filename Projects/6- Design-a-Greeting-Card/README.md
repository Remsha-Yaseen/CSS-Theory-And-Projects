# 🥳 Interactive Birthday Card
A stylish, responsive, and interactive greeting card built entirely with HTML and CSS. This project demonstrates advanced CSS techniques to create a smooth user experience without the need for JavaScript.

----

### 🚀 Key Features

* **Dynamic Hover Effects:** Smooth scaling and color transitions using the transition and transform properties.

* **Pure CSS Navigation:** Utilizes the :target pseudo-class to show/hide sections (Send/Share) dynamically.

* **Modern Layout:** Built with Flexbox for perfectly aligned links and responsive centering.

* **Accessibility Focused:** Includes custom :focus states for keyboard navigation and uses relative units (em) for better scaling.

* **Decorative Pseudo-elements**: Uses ::before and ::after to insert decorative emojis without cluttering the HTML.


### 🛠️ Technologies Used

* **HTML5:** Semantic structure.

* **CSS3:** Custom styling, transitions, and interactive pseudo-classes (:hover, :active, :target).
