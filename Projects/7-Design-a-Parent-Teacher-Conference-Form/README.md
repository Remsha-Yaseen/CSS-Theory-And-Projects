# Modern Student Registration Form

A sleek, responsive registration form built with semantic HTML5 and advanced CSS3. This project focuses on custom UI components and accessible user experience.

----


## 🚀 Key Features
* **Custom UI:** Hand-crafted radio buttons using `::before` pseudo-elements and CSS transitions.
* **Modern Layout:** Vertical form flow using the `:not()` negation pseudo-class for layout control.
* **Responsive Design:** Built with `rem` units to ensure accessibility and scaling across devices.
* **Interactive Elements:** Smooth hover states and "blooming" selection animations for better UX.

## 🛠️ Tech Stack
* **HTML5:** Semantic structure (fieldset, legend, labels).
* **CSS3:** Flexbox, Pseudo-selectors (`:checked`, `:not`), and CSS Transforms.
